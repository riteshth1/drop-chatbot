stages:
  - build
  - deploy

build:
  stage: build
  image: docker:20.10.16
  services:
    - docker:20.10.16-dind
  variables:
    DOCKER_TLS_CERTDIR: "/certs"
  script:
    - docker build -t registry.gitlab.com/username/drop-chatbot:latest .

deploy:
  stage: deploy
  image: alpine:latest
  script:
    - echo "Deploying to Render..."
    - export BASE_URL="https://drop-chatbot.onrender.com"
    - # Render deployment steps would continue here
